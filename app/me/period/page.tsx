"use client";

import PeriodPage from "@/app/period/page";

/**
 * Owner-side period tracking page.
 * Reuses partner-side component since period data is shared.
 */
export default function MePeriodPage() {
  return <PeriodPage />;
}