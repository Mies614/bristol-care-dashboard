export type {
  AppData,
  CloudSettings,
  CommonLink,
  Course,
  DayName,
  Deadline,
  LoveNote,
} from "@/lib/types";

/** @deprecated Use CommonLink instead */
export type { QuickLink } from "@/lib/types";